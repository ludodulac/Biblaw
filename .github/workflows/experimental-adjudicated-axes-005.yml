name: Experimental adjudicated axes 005
on:
  push:
    branches: [experiment-adjudicated-axes-contract-005]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python tests/test_adjudicated_thematic_axes.py
