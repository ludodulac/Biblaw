name: Experimental thematic consolidation 002
on:
  push:
    branches:
      - experiment-thematic-consolidation-002
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: python tests/test_thematic_consolidation.py
